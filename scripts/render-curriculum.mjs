import { a101, a101Sources } from '../curriculum/a101.js';
import { modules } from '../curriculum/schedules.js';
import { referenceWeeklyHours } from '../src/schedule.js';
import { stage1Modules, stage1Routes, a101Boundaries } from '../curriculum/stage1.js';
import { computing } from '../curriculum/m101-computing.js';
import fs from 'node:fs';
import { fileURLToPath } from 'node:url';
import { evidence, review } from '../curriculum/m101-review.js';
import { m101 as m } from '../curriculum/m101.js';
import { sources, coverage, gaps } from '../curriculum/coverage.js';

const root = fileURLToPath(new URL('../', import.meta.url));
const e = value => String(value).replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;');
const modulePath='/programme/stage-1/lu-m101/';
const unitHours=m.units.reduce((sum,u)=>sum+u.hours,0);
const extraHours=m.additionalWork.reduce((sum,u)=>sum+u.hours,0);
const link=(path,label)=>`<a href="${e(path)}">${e(label)}</a>`;
const section=(id,label,title,body)=>`<section id="${id}" class="programme-section"><p class="eyebrow">${label}</p><h2>${title}</h2>${body}</section>`;
const table=(caption,head,rows)=>`<div class="table-scroll" tabindex="0" role="region" aria-label="${e(caption)}"><table><caption>${e(caption)}</caption><thead><tr>${head.map(h=>`<th scope="col">${e(h)}</th>`).join('')}</tr></thead><tbody>${rows.map(row=>`<tr>${row.map((cell,i)=>i===0?`<th scope="row">${cell}</th>`:`<td>${cell}</td>`).join('')}</tr>`).join('')}</tbody></table></div>`;
const footer=`<footer><a href="/identity/">LibraUni · Our identity</a><a class="licence-link" href="/license/"><span>Licence &amp; reuse</span><img src="/licenses/cc-by-nc-sa.svg" width="88" height="31" alt="CC BY-NC-SA 4.0 — course materials; exclusions apply"></a><span>Independent learning · Our awards are non-accredited.</span></footer>`;
const shell=(title,crumbs,body)=>`<!doctype html>
<html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="description" content="${e(title)} — LibraUni curriculum design and module preview."><meta name="theme-color" content="#F7F4ED"><title>${e(title)} · LibraUni</title><link rel="icon" href="/brand/favicon.svg"><script>try{if(localStorage.getItem('librauni-theme')==='dark')document.documentElement.dataset.theme='dark'}catch{}</script><link rel="stylesheet" href="/src/style.css"><link rel="stylesheet" href="/src/identity.css"><link rel="stylesheet" href="/src/programme.css"></head>
<body><a class="skip" href="#main">Skip to content</a><header><a class="brand" href="/" aria-label="LibraUni home"><span class="lu-symbol" aria-hidden="true"><img src="/brand/librauni-symbol-transparent.png" alt="" width="864" height="524"></span><span>LibraUni</span></a><span class="header-label">INDEPENDENT STUDY · PHYSICS</span><div class="header-actions"><button id="theme-toggle" class="theme-button" aria-pressed="false">Dark mode</button><a class="button-link" href="/">Study desk</a></div></header><main id="main" class="programme-page"><nav class="breadcrumb" aria-label="Breadcrumb">${crumbs.join('<span aria-hidden="true">/</span>')}</nav>${body}${footer}</main><script type="module" src="/src/theme.js"></script></body></html>`;

const outcomeTable=table('Proposed module learning outcomes',['Outcome','What completion should demonstrate'],m.outcomes.map(([id,title,detail])=>[`${e(id)} · ${e(title)}`,e(detail)]));
const unitTable=table('All 12 units — select a title for its specification',['Unit','Hours','Role'],m.units.map(u=>[link('#'+u.id,`${u.id} · ${u.title}`),e(u.hours),e(u.can)]));
const unitCards=m.units.map(u=>`<article class="unit-card" id="${u.id}" aria-labelledby="title-${u.id}"><p class="eyebrow">${u.id} · ${u.hours} HOURS · OUTLINE ONLY</p><h3 id="title-${u.id}">${e(u.title)}</h3><p><strong>Before this unit:</strong> ${u.requires.length?u.requires.map(id=>link('#'+id,id)).join(', '):'Module entry readiness; no previous unit.'}</p><dl><dt>Scope</dt><dd>${e(u.scope)}</dd><dt>By the end</dt><dd>${e(u.can)}</dd><dt>Computing strand</dt><dd>${e(u.computing)}</dd><dt>Physical context</dt><dd>${e(u.context)}</dd><dt>Evidence to look for</dt><dd>${e(u.evidence)}</dd><dt>Boundary with later study</dt><dd>${e(u.boundary)}</dd></dl><p class="small">Contributes to ${u.outcomes.map(id=>link('#outcomes',id)).join(', ')}. Activities and assessment questions will be authored after the Stage 1 design and readiness review.</p><a class="small" href="#units">Back to unit map ↑</a></article>`).join('');
const workload=table('Complete workload budget — no extra required course hidden outside it',['Component','Hours','What is included'],[
 ['Twelve units',String(unitHours),'Reading and historical context, worked examples, required practice, scaffolded Python work and formative checkpoints. These hours exclude the separately listed submissions below.'],
 ...m.additionalWork.map(w=>[e(w.name),String(w.hours),e(w.detail)]),
 ['Total',String(unitHours+extraHours),`${m.credits} internal credits × 10 notional hours.`],
]);
const assessments=table('Assessment-to-outcome map — formats proposed for review',['Evidence','After / drawing on','Outcomes'],m.assessment.map(a=>[e(a.name),e(a.units.length===12?'All units':a.units.join(', ')),e(a.outcomes.join(', '))]));
const boundaries=table('First teaching versus purposeful reuse',['Subject','This module owns','Later use / new depth'],[
 ['Algebra and elementary functions','Fluent manipulation, domains, equations and graphical interpretation.','M102 uses these for limits and calculus. An entry check and targeted repair replace a second full algebra course.'],
 ['Trigonometry','Geometry, radians, basic identities and periodic functions.','M102/M103 extend identities and calculus; P201 derives physical oscillation and wave models.'],
 ['Vectors','Geometric two-dimensional components and projections.','M102/M103 develop algebraic structure and three-dimensional tools; M201 develops vector fields and calculus.'],
 ['Data and probability','Descriptive summaries and simple finite-event reasoning.','X201 develops inference, uncertainty and fitting; P201 needs a separately specified statistical-physics foundation.'],
 ['Python','Readable calculations, functions, labelled plots and reproducible small notebooks.','M201/X201 add numerical methods and validation. Optional C301 develops advanced numerical physics.'],
 ['Physics','Supplied relationships motivate mathematics.','P101 teaches the physical laws; mathematical examples here do not count as independent physics-course coverage.'],
]);
const moduleBody=`<section class="intro"><p class="eyebrow">STAGE 1 / ${m.code} / PREVIEW ${m.version}</p><h1>${m.title}</h1><p class="lead">${m.purpose}</p><p class="small">${m.status}<br>${m.date} · ${m.credits} internal credits · ${unitHours+extraHours} notional hours</p></section>
<div class="notice">The maths/Python remit has been revised following the first design review. M101 can serve as preparation outside the astronomy route or count within the foundation route. The revised scope and reference timetable require review before release; no enrolment is implied.</div>
<nav class="programme-nav" aria-label="Module preview sections">${[['study-planner','Study planner'],['place','Place in the degree'],['outcomes','Outcomes'],['units','Unit map'],['workload','Workload'],['assessment','Assessment'],['computing','Python & AI'],['boundaries','Boundaries'],['review','Review']].map(([id,title])=>link('#'+id,title)).join('')}</nav>
<section id="study-planner" class="programme-section" data-planner="LU-M101"><h2>Module planner</h2><p>Loading your study planner…</p></section><script type="module" src="/src/planner.js"></script>
${section('place','01 / PURPOSE & READINESS','A foundation with a clear destination.',`<p><strong>Entry:</strong> ${m.entry}</p><p><strong>Exit:</strong> ${m.exit}</p><div class="learning-chain" aria-label="Learning progression"><span>Everyday quantitative reasoning</span><span aria-hidden="true">→</span><strong>M101 · mathematical fluency</strong><span aria-hidden="true">→</span><span>M102 · calculus &amp; P101 · physical models</span></div><p>Our reference comparison distinguishes entry knowledge from degree content. ${link(sources.find(s=>s.id==='MIT-C1').url,'MIT’s 18.01SC syllabus')} assumes algebra and trigonometry before calculus. M101 deliberately teaches those foundations rather than presenting them as a substitute for calculus. ${link(sources.find(s=>s.id==='OU-F').url,'OU MU123')} is a useful foundations reference; this original outline is not a copy or a certified equivalent.</p><p>If later readiness evidence makes the full module unnecessary, the programme allows an alternative Stage 1 route, subject to review. No exemption has been awarded and no diagnostic is being set here.</p>`)}
${section('outcomes','02 / LEARNING OUTCOMES','What you should be able to do.',outcomeTable)}
${section('units','03 / COMPLETE UNIT MAP','Twelve connected units.',`<p>The recommended order is U01–U12. Dependencies below identify the essential earlier tools; they are not a demand to study several units concurrently. Each hour estimate includes that unit’s required practice and computing. Optional enrichment will be labelled separately and never assessed without being budgeted.</p>${unitTable}<p class="small">${unitHours} hours of unit work + ${extraHours} hours of orientation, submissions, revision, final assessment and feedback = ${unitHours+extraHours} hours.</p>${unitCards}`)}
${section('workload','04 / EXPECTED EFFORT','A real 300-hour allocation.',`${workload}<p>The current LibraUni draft allocates 30 study weeks, averaging 10 hours a week; exact OU timetable fidelity remains unresolved (see the review below). Dates and assessments are pre-scheduled from your chosen start date. You may move dates or add a break through the planner. Concurrent modules add to the weekly workload; their dates do not automatically stretch.</p><p>These are design estimates, not attendance requirements. The reference workload must follow the relevant OU module structure, including assessment peaks; it is not capped or smoothed to match a learner’s available weekly hours. Personal delays are explicit timetable changes. Actual time depends on readiness and practice needs. The first taught material will test these estimates, especially the dense trigonometry unit. Any change to scope or hours will be visible before it becomes a new requirement.</p>`)}
${section('assessment','05 / EVIDENCE & FEEDBACK','Understanding, not just page completion.',`<p>Short formative checks belong within each unit and include explanations of errors. The separate assessments below sample independent reasoning, not recognition of worked solutions. All questions and marking guides remain to be written after the Stage 1 design and readiness review; the planner sets target dates that you can adjust.</p>${assessments}${table('Independent evidence required for every outcome',['Outcome','Assessment home','Evidence required'],evidence.map(row=>row.map(e)))}<p><strong>Assessment vocabulary:</strong> TMA means tutor-marked assignment; iCMA means interactive computer-marked assignment; EMA means end-of-module assessment. Three one-hour iCMAs are scheduled within existing unit checkpoint time, so they do not add hours. M101 ends with one EMA combining a Python modelling notebook, handwritten mathematics submitted as a scanned PDF, and explanation/discussion. There is one EMA deadline and no separate final examination. Papers, questions, submission links, allowed aids and grading rules will be published before use; no live assessment or upload is available yet.</p><p><strong>Proposed completion rule:</strong> every module outcome must be demonstrated through independent work and the combined EMA, including explained Python results. Reading completion or a strong average must not conceal an unresolved foundational outcome. The review vocabulary is “not yet demonstrated”, “demonstrated in familiar work” and “demonstrated in transfer”; a final rubric and any numerical grade policy must be agreed before assessment begins.</p><p>Routine practice may use tutor support. Independent submissions must state the tools and assistance used, and must be followed by explanation or a fresh task where authorship or understanding is unclear. Feedback leads to targeted repair and reassessment, not an invented pass. All submissions and attainment records remain private.</p>`)}
${section('computing','06 / SCIENTIFIC PROGRAMMING','Write it. Understand it. Then delegate carefully.',`<p>The foundations begin with learner-written code and deliberate practice. Later work introduces AI assistance and bounded coding agents, with responsibility for the model, code and results staying with the learner. Abstract mathematical exercises remain; physics supplies context when it helps.</p><p><strong>Budget:</strong> 48 of the existing 240 unit hours are allocated to integrated mathematical computing; 192 remain for other unit study. These are not 48 extra hours, and computing still practises the mathematics. TMA 02 and the EMA retain their separate existing budgets. This is a revised design allocation to check during authoring, not a measured learning-time claim.</p>${table('Python and AI progression within the existing unit budget',['Unit','Integrated hours','Focus','Expected practice'],computing.map(([id,h,title,detail])=>[link('#'+id,id),e(h),e(title),e(detail)]))}<p><strong>Assessment:</strong> early fluency tasks require learner-written code. TMA 02 checks a short notebook with an independent hand check. The EMA must include explaining and debugging a small piece of code, reviewing a proposed change and defending the final notebook. Assistance rules will distinguish unaided, supported and assisted components before release. Generated code that runs is not evidence of understanding.</p><p><strong>Boundaries:</strong> this prepares scientific programming, not a full computer-science or agent-engineering course. Advanced software architecture, model training and multi-agent systems are outside the remit. Current tools and free access will be verified during authoring; required work must remain possible without buying AI access. A supplied agent trace supports critical review but does not count as hands-on agent operation.</p>`)}
${section('boundaries','07 / COHERENCE','Teach once. Return with a new purpose.',`${boundaries}<p>Complex numbers, calculus, matrices/eigenproblems, differential equations, advanced probability and physical law derivations are intentionally outside M101. They are tracked in the ${link('/programme/coverage/','programme coverage register')}; absence from this module is not permission to omit them from the degree.</p><p>Every eventual unit will include orientation, original explanations, worked examples, graduated activities, supporting diagrams with captions, a concise reference summary and a review of its outcomes. Relevant historical context belongs in the allotted reading time. Python supports the reasoning and never replaces a mathematical explanation.</p>`)}
${section('review','08 / DESIGN REVIEW · 24 SEPTEMBER 2026','A reviewed foundation, with explicit release gates.',`<p><strong>Previous review:</strong> version 0.4 retained the twelve-unit foundation and combined EMA. Version 0.5 now makes Python foundations and controlled AI assistance explicit; this revision is open for review. The timetable discrepancy below remains open.</p>${table('Review decisions and outstanding conditions',['Area','Disposition','Decision / required action'],review.map(row=>row.map(e)))}<p><strong>Reference check:</strong> ${link('https://www.open.ac.uk/courses/qualifications/details/mu123/','Current OU MU123 guidance')} and ${link('https://www.open.ac.uk/library/digital-archive/module/xcri:MU123/','historical MU123 archive')} must not be treated as the same presentation. The linked sample calendar could not be retrieved (HTTP 403); no full week-by-week equivalence is claimed. ${link(sources.find(s=>s.id==='MIT-C1').url,'MIT calculus entry requirements')} support the algebra/trigonometry handoff, not institutional equivalence.</p><p><strong>Next:</strong> review this revised preparation remit, review the new A101 unit outline, then define M102, M103 and P101 individually. Reconcile Stage 1 prerequisites and coverage before assessing readiness to produce M101 lessons, one lesson or smaller portion at a time. No start date or enrolment is inferred.</p><p class="small">Version 0.5. The original scope review is retained below as a baseline; the expanded Python remit above now needs a further workload and assessment review. Scope and assessment review of version 0.4 completed; teaching quality, delivery workload and learner mastery have not yet been tested. Wider gaps remain in the ${link('/programme/coverage/','coverage register')}.</p>`)}
`;

const citeIds=text=>e(text).replace(/MIT-C1|MIT-C2|MIT-D|MIT-S|IC-D|IC-P|OU-F/g,id=>link('#source-'+id,id));
const coverageRows=coverage.map(([id,subject,refs,dest,status,action])=>[
 `<span id="${id}">${id} · ${e(subject)}</span>`,citeIds(refs),e(dest),`<strong>${e(status)}</strong><br>${e(action)}`,
]);
const auditBody=`<section class="intro"><p class="eyebrow">PHYSICS / COVERAGE REGISTER 0.1</p><h1>Make the coverage visible.</h1><p class="lead">A subject has a place only when its outcomes, prerequisites, study time and assessment have a place.</p><p class="small">First comparison · 23 September 2026 · Module-level destinations remain proposals. M101 has a reviewed mathematical outline and a revised Python strand awaiting review; timetable approval remains open. A101 now has a <a href="/programme/stage-1/lu-a101/">paired-study unit outline</a> for review.</p></section>
<nav class="programme-nav" aria-label="Coverage sections">${[['method','Method'],['findings','Findings'],['matrix','Coverage map'],['m101','M101 audit'],['sources','Sources']].map(([id,t])=>link('#'+id,t)).join('')}</nav>
${section('method','01 / METHOD','Compare content, not prestige or titles.',`<p>The OU provides the organisational model. MIT Course 8 supplies a demanding physics-depth reference; Imperial’s Physics BSc supplies a second, UK-style breadth reference. These programmes have different entry preparation, credit conventions and elective structures. We do not equate their first year with our foundation stage or convert their credits into ours.</p><p>This is an initial subject-level comparison, not a declaration that the degree is complete or equivalent. The outcome descriptions and destinations below are our design judgments derived from the published scope. A full match requires detailed unit outcomes, expected demand, prerequisites, hours and assessed evidence.</p><p><strong>Status key:</strong> “Revised outline” means a previous design has changed and needs a further review. “Reviewed outline” means the unit and assessment design has been reviewed, with any release gates stated on its module page. “Outlined” means a draft unit-level home exists, not that teaching exists or mastery has been demonstrated. “Pending” means only a module-level home is proposed. “Gap to resolve” means the blueprint lacks an explicit required allocation. “Depth to resolve” means a subject is named but its depth and workload are unproven. Practical limitations and deliberately optional subjects are separate.</p><p>We compare the shared undergraduate physics foundation plus selected specialisations, not every elective or every university-wide requirement. Source dates and retrieval limits are recorded below.</p>`)}
${section('findings','02 / FIRST FINDINGS','The broad blueprint needs sharper boundaries.',`<p>MIT’s focused route includes analytical mechanics and an extended quantum sequence. Imperial’s published core includes solid-state and nuclear/particle physics. These checks expose vague or missing homes in our blueprint; they do not justify simply adding more topics to an unchanged timetable.</p><p>${link('#source-MIT-D','MIT degree reference')} · ${link('#source-IC-P','Imperial course reference')}</p>${gaps.map(g=>`<article class="unit-card" id="${g.id}"><p class="eyebrow">${g.id} · OPEN</p><h3>${e(g.title)}</h3><p>${e(g.decision)}</p><p class="small">Coverage rows: ${g.rows.map(id=>link('#'+id,id)).join(', ')}.</p></article>`).join('')}<p>These are tracked decisions for later module design, not silent changes to the agreed 360-credit architecture. Resolving a gap requires an explicit curriculum and workload decision.</p>`)}
${section('matrix','03 / PROGRAMME MAP','Where each subject is intended to live.',table('Initial subject coverage and unresolved decisions',['Subject','Benchmark anchors','Proposed LibraUni destination','Status and required action'],coverageRows))}
${section('m101','04 / FIRST MODULE AUDIT','A defined precalculus foundation.',`<p>${link(modulePath,'LU-M101 Mathematical foundations')} now has twelve reviewed units and seven assessable outcomes. Its role is preparatory. It does not claim to deliver MIT calculus or Imperial first-year physics.</p>${table('M101 outline-level traceability',['Requirement','Unit allocation','Evidence / handoff'],[
 ['Numerical and dimensional fluency','U01–U02; reused throughout','O1/O5, TMA 01 and the EMA; transferred to P101.'],
 ['Algebra, equations and elementary functions','U03–U07 and U09','O2/O3, TMA 01/02 and the EMA; prepares M102 calculus.'],
 ['Geometry and trigonometry','U08, supported by U02/U06/U07','O4, TMA 03 and the EMA; prepares vector and wave work.'],
 ['Elementary vector reasoning','U10 after U05/U08','O4, TMA 03 and the EMA; geometric entry to later linear algebra.'],
 ['Data/probability orientation','U11, supported by U02/U05/U06','O5/O6, notebook and the EMA. C13 remains open for later depth.'],
 ['Computing and explanation','Across units; integration in U12','O6/O7, TMA 02 notebook and EMA notebook/discussion; revision 0.5 adds explicit programming and AI-review expectations within unit budgets. The preview separates assisted practice from independent evidence.'],
 ])}<p>Intentional overlap is documented: M101 teaches algebra and graphs; later modules retrieve them to introduce new mathematics or derive physical laws. Repeating the same introductory course would need a specific remediation reason, not a second allocation by default.</p><p>See the ${link(modulePath+'#review','M101 design review')} for the open timetable discrepancy and authoring gates.</p><p><strong>Outline checks:</strong> 240 unit hours plus 60 module-wide hours; 300 total. Every outcome has a unit home and independent assessment home. Dependencies refer to earlier units. These structural checks do not establish lesson quality, learner attainment or a complete degree audit.</p>`)}
${section('sources','05 / EVIDENCE REGISTER','Sources and limits of verification.',`<p>Only institutional sources are used. Some Imperial pages refused direct retrieval; their indexed official descriptions support the limited comparison stated here. Before final approval of later modules, detailed syllabi and assessments must be checked. No claim of full Imperial outcome verification is made.</p><ol class="source-list">${sources.map(s=>`<li id="source-${s.id}"><strong>${s.id}</strong> · ${link(s.url,s.name)}<p>${e(s.note)}</p></li>`).join('')}</ol><p class="small">Version 0.1. Keep this register updated as each module is reviewed. Future rows should add exact unit destinations and assessment evidence; an unresolved gap must never be silently relabelled as covered.</p>`)}
`;
for(const [path,title,crumbs,body] of [
 ['programme/stage-1/lu-m101/index.html','Mathematical foundations',[link('/programme/','Physics'),link('/programme/#stage-1','Stage 1'),'<span aria-current="page">LU-M101</span>'],moduleBody],
 ['programme/coverage/index.html','Curriculum coverage',[link('/programme/','Physics'),'<span aria-current="page">Coverage register</span>'],auditBody],
]){fs.mkdirSync(root+path.slice(0,path.lastIndexOf('/')),{recursive:true});fs.writeFileSync(root+path,shell(title,crumbs,body));}

// Keep route membership and credit arithmetic in one structured source.
const routeRows=stage1Routes.map(r=>[e(r.title),r.codes.map(e).join(' + '),String(r.codes.reduce((sum,id)=>sum+stage1Modules.find(m=>m.code===id).credits,0)),e(r.preparation)]);
const stage1Body=section('stage-1','02 / STAGE 1','Two entry routes · 120 credits each.',`<p>The astronomy route places astronomy and computational investigation within Stage 1, with optional preparation before or alongside it. The foundation route retains a full mathematics foundation within the stage. Both protect M102, M103 and P101; neither awards credit merely for prior familiarity.</p>${table('Stage 1 routes',['Route','Modules','Credits','Preparation / additional study'],routeRows)}${table('Module roles — all modules are 30 internal credits',['Module','Purpose'],stage1Modules.map(m=>[m.path?link(m.path,m.code+' · '+m.title):e(m.code+' · '+m.title),e(m.purpose)]))}<p><strong>Preparation and workload:</strong> the full M101 remains 300 notional hours. Taken outside the astronomy route, it makes preparation plus Stage 1 1,500 hours; the degree still counts 120 credits at this stage and 360 overall. Targeted refreshers have a separately scoped workload and do not earn the full M101 credit. Completion of a full additional module can be recorded separately; never count it twice towards a degree award.</p><p><strong>Readiness, not repetition:</strong> a diagnostic followed by a fuller mastery check will identify preparation needs. Evidence may justify selected refreshers instead of the full M101. Mathematics and Python readiness are checked separately. No exemption, completion, start date or enrolment is inferred from this route design.</p><p><strong>Sequence:</strong> establish M101-level readiness before M102; M102 prepares for M103. P101 and mathematics may be coordinated once exact unit prerequisites exist. A101 can start alongside M101 using its new unit-level readiness gates and introductory physical context. This supersedes the earlier provisional requirement to finish P101/M102 first; no calculus or completed physics module is assumed in A101 0.1.</p><h3 id="a101-brief">LU-A101 · Exploring astronomy through computation</h3><p>A complete <a href="/programme/stage-1/lu-a101/">A101 unit-level preview and paired timetable</a> is now available for review. It adds an observational and investigative strand; it does not displace the compulsory mathematics or general physics foundation.</p>${table('A101 remit and boundaries',['Area','Design boundary'],a101Boundaries.map(row=>row.map(e)))}<p><strong>Next design sequence:</strong> review M101’s revised preparation remit; review A101’s unit-level design; then develop M102, M103 and P101 individually. Reconcile the full Stage 1 coverage, dependencies and workload before releasing teaching. Selecting a route is separate from enrolling in modules.</p>`);
const programmeFile=root+'programme/index.html';
let programme=fs.readFileSync(programmeFile,'utf8');
programme=programme.replace(/<section id="stage-1"[\s\S]*?(?=<section id="stage-2")/,stage1Body+'\n');
programme=programme.replace('PROGRAMME BLUEPRINT 0.1','PROGRAMME BLUEPRINT 0.2').replace('Draft for discussion · 23 September 2026','Route architecture revised · 24 September 2026');
programme=programme.replace('First module ready for outline review:', 'Revised preparation module for review:');
fs.writeFileSync(programmeFile,programme);

const aPath='/programme/stage-1/lu-a101/';
const aSchedule=modules.find(m=>m.code===a101.code),mSchedule=modules.find(m=>m.code==='LU-M101');
const paired=Array.from({length:31},(_,i)=>{const w=i+1;return {w,m:referenceWeeklyHours(mSchedule,w),a:referenceWeeklyHours(aSchedule,w)};});
const peak=Math.max(...paired.map(r=>r.m+r.a));
const aBody=`<section class="intro"><p class="eyebrow">STAGE 1 / LU-A101 / PREVIEW ${a101.version}</p><h1>${e(a101.title)}</h1><p class="lead">${e(a101.purpose)}</p><p class="small">24 September 2026 · 30 internal credits · 300 notional hours · Unit outline proposed for review</p></section><p class="notice">Designed as a possible first-module pairing with M101. This is a complete unit-level proposal, not available lessons or an enrolment. The 31-study-week timetable is provisional; exact OU calendar fidelity remains open.</p><nav class="programme-nav" aria-label="Module preview sections">${[['place','Readiness'],['outcomes','Outcomes'],['units','Units'],['pairing','M101 pairing'],['assessment','Assessment'],['practical','Tools & evidence'],['study-planner','Planner'],['review','Review & sources']].map(([id,t])=>link('#'+id,t)).join('')}</nav>
${section('place','01 / ROLE & READINESS','Astronomy alongside your foundations.',`<p>${e(a101.entry)}</p><p>The module teaches the minimum astronomical context needed for its observations: how light is measured, what a spectrum records, and what a supplied model assumes. It does not assume P101 or M102 as completed modules. P101 later develops the physical laws systematically; M102/M103 supply calculus and further mathematics. Their content is not counted as covered merely because an astronomical example uses a supplied relation.</p><p>The endpoint is a small, independently explained astronomical investigation. It prepares questions and data habits for later astrophysics, not advanced stellar structure, orbital dynamics or cosmology.</p>`)}
${section('outcomes','02 / OUTCOMES','From a sky view to a defensible claim.',table('A101 learning outcomes',['Outcome','Evidence of completion'],a101.outcomes.map(([id,t,d])=>[e(id+' · '+t),e(d)])))}
${section('units','03 / UNIT MAP','Ten connected investigations.',`<p>240 unit hours include explanations, relevant history, worked examples, required practice, scaffolded practical work and three one-hour iCMAs. The other 60 hours cover orientation, TMAs, revision, feedback and the EMA. No extra required activity is hidden as optional enrichment.</p>${table('Complete unit map',['Unit','Weeks','Hours'],a101.units.map(u=>[link('#'+u.id,u.id+' · '+u.title),u.startWeek+'–'+u.endWeek,String(u.hours)]))}${a101.units.map(u=>`<article class="unit-card" id="${u.id}"><p class="eyebrow">${u.id} · WEEKS ${u.startWeek}–${u.endWeek} · ${u.hours} HOURS</p><h3>${e(u.title)}</h3><p><strong>Before this unit:</strong> ${u.requires.map(id=>link('#'+id,'A101 '+id)).join(', ')||'Module entry readiness'}. ${u.external.length?'M101 preparation: '+u.external.map(id=>link(modulePath+'#'+id,id)).join(', ')+', or demonstrated equivalent readiness.':''}</p><dl><dt>Scope</dt><dd>${e(u.scope)}</dd><dt>Practical investigation</dt><dd>${e(u.activity)}</dd><dt>Computing progression</dt><dd>${e(u.computing)}</dd><dt>Evidence</dt><dd>${e(u.evidence)}</dd><dt>Boundary</dt><dd>${e(u.boundary)}</dd></dl><p class="small">Outcomes ${u.outcomes.join(', ')}. Original lessons, diagrams, questions and selected data will be prepared after design review.</p><a href="#units">Back to unit map ↑</a></article>`).join('')}`)}
${section('pairing','04 / CONCURRENT STUDY','Two modules, one checked preparation sequence.',`<p>The baseline assumes both modules start together. Astronomy interpretation comes first; independent plotting waits until week 12, functions until week 15, parallax trigonometry until week 21, logarithmic magnitudes until week 23, and descriptive statistics until week 27. This avoids asking A101 to teach M101 again.</p>${table('Cross-module readiness gates',['A101 unit starts','M101 preparation finishes','What becomes usable'],a101.units.filter(u=>u.external.length).map(u=>[`${u.id} · week ${u.startWeek}`,u.external.map(id=>`${id} · week ${mSchedule.events.find(r=>r.id===id).endWeek}`).join('; '),e(u.computing)]))}<p><strong>Combined commitment:</strong> 600 notional hours across the two full modules, approximately ${(600/31).toFixed(1)} hours/week across the 31-week planning horizon. The current peak is approximately ${peak.toFixed(1)} hours in week(s) ${paired.filter(r=>Math.abs(r.m+r.a-peak)<0.001).map(r=>r.w).join(', ')}. This describes the draft, not a cap or a promise of actual study time. Personal changes remain your choice.</p><details><summary>All 31 weeks: combined workload and assessment dates</summary>${table('Same-start baseline workload',['Week','M101 hours','A101 hours','Combined','Assessments due'],paired.map(r=>[String(r.w),r.m.toFixed(1),r.a.toFixed(1),(r.m+r.a).toFixed(1),[mSchedule,aSchedule].flatMap(m=>m.events.filter(x=>['TMA','iCMA','EMA','exam'].includes(x.type)&&x.endWeek===r.w).map(x=>e(m.code+' '+x.title))).join('; ')||'—']))}</details><p>M101’s EMA ends in week 30; A101’s ends in week 31, with some preparation overlapping. Their scopes and submissions are separate. The planner warns when saved date changes put an M101 prerequisite after the start of its dependent A101 unit; it never treats elapsed dates as demonstrated mastery. If readiness is demonstrated independently, review the warning with the tutor rather than inventing completion.</p><p>The readiness gates depend on M101’s current provisional timetable. Resolve its outstanding OU-reference discrepancy and recheck this pairing before opening either presentation.</p>`)}
${section('assessment','05 / ASSESSMENT & WORKLOAD','Explain the result and show how you know.',`${table('All required work',['Component','Hours','Included work'],[['Ten units','240','Original teaching, practice, practicals and three iCMAs; not additional to this budget.'],['Orientation','6','Astronomy workspace, provenance and access checks; coordinate general setup with M101.'],['Three TMAs','18','Six hours each; cut-offs in weeks 4, 14 and 23.'],['One EMA','18','Notebook 10h; written/scanned mathematics and interpretation 5h; preparation/discussion 3h.'],['Revision','12','Mixed interpretation and method checks distributed across the presentation.'],['Feedback and corrections','6','Two hours after each TMA.'],['Total','300','30 internal credits × 10 notional hours.']])}${table('Assessment blueprint',['Assessment / due week','Preparation','Outcomes','Independent evidence'],a101.assessments.map(a=>[e(a.name)+' · week '+a.week,a.requires.map(id=>link('#'+id,id)).join(', '),a.outcomes.join(', '),e(a.evidence)]))}<p>There is no separate final examination in this proposal. The EMA is one submission with identified components. Formative iCMAs provide explained feedback; completion alone cannot establish all outcomes. Tutor-marked work and the EMA must demonstrate all six outcomes independently, with targeted reassessment where necessary. Task-level rubrics, any numerical grade policy, allowed assistance and private submission arrangements will be published before use.</p><p>Early coding checks are learner-written. Later assisted work must include an assistance record, reviewed changes, independent numerical checks and an explanation of the code. A short fresh explanation or repair task can clarify understanding. Tool-generated output is not itself evidence of mastery. Handwritten mathematics and scans are included where appropriate within the stated budgets.</p>`)}
${section('practical','06 / PRACTICAL EXPERIENCE','Small data, substantial reasoning.',`<p>Planned tools are local Python notebooks, NumPy and Matplotlib, followed by selected Astropy unit/table/FITS helpers. No paid software, AI account, GPU or telescope is required. Use small local datasets and provide a documented offline path; live archive access is an extension of the reproducible workflow, not a fragile prerequisite for every session.</p><p>Gaia catalogue subsets support positions, parallax and population diagrams; MAST/TESS light curves support variability. Image and spectral cutouts will be selected and rights-checked before lesson release. Each released dataset needs its source, product/release, fields, units, filters, acknowledgements, retrieval/query recipe and limitations. Derived teaching files must retain provenance and third-party terms; public access alone is not permission to relicense them.</p><p>Simulations will expose assumptions: change one quantity, predict an effect, measure it, compare with the model and explain discrepancies. Planned practical evidence includes a source/background comparison, spectral-shift check, transit-candidate critique, parallax selection audit and population diagram. No discovery claim, instrumental competence or advanced uncertainty analysis is implied.</p><p>Before launch, test a pinned compatible environment and representative datasets on an ordinary laptop. Exact dataset selections, download sizes and setup instructions are not yet released. AI or agents may help inspect or refactor a bounded piece of work after foundations; their changes must be reviewed and independently tested.</p>`)}
<section id="study-planner" class="programme-section" data-planner="LU-A101"><h2>Module planner</h2><p>Loading your study planner…</p></section><script type="module" src="/src/planner.js"></script>
${section('review','07 / DESIGN REVIEW','A first-module design, not an advanced-course shortcut.',`<p><strong>Proposed for review:</strong> ten units, 300 hours, three TMAs, three iCMAs and one EMA. The mathematics/Python pairing is checked against M101’s present baseline; both remain closed to enrolment until materials and the presentation are ready. No start date has been set.</p><p><strong>Benchmark boundary:</strong> MIT’s astronomy syllabus expects mechanics and calculus. Imperial’s advanced astrophysics scope is a later-depth reference. A101 concentrates on introductory observational evidence; later modules still need orbital dynamics, stellar structure/evolution, compact objects, galactic dynamics and cosmology. P101 must teach general physics rather than repeat these archive investigations. See the ${link('/programme/coverage/','coverage register')} for the wider unresolved allocations.</p><p><strong>Timing limitation:</strong> the OU S284 page identifies an assessment in study week 31. We use 31 study weeks as a provisional horizon, not as proof of its full calendar length or weekly workload. A101’s units and EMA differ from S284. Exact OU-reference reconciliation remains required before final timetable approval, rather than claiming that the paired design reproduces an OU presentation.</p><p><strong>Release checks:</strong> review unit density and practical allocations, obtain a fuller reference calendar, verify data/tool access and permissions, pilot representative activities, finish marking/submission arrangements, and recheck cross-module prerequisites after any M101 change. These are design conditions, not tasks already completed.</p><ol class="source-list">${a101Sources.map(([title,url,note])=>`<li>${link(url,title)}<p>${e(note)}</p></li>`).join('')}</ol><p>Next: review A101 before proceeding to M102, then M103 and P101 individually. The full Stage 1 audit precedes lesson production.</p>`)}
`;
fs.mkdirSync(root+'programme/stage-1/lu-a101',{recursive:true});
fs.writeFileSync(root+'programme/stage-1/lu-a101/index.html',shell(a101.title,[link('/programme/','Physics'),link('/programme/#stage-1','Stage 1'),'<span aria-current="page">LU-A101</span>'],aBody));
